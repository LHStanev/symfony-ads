{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ page.title }}</h1>
    <div>{{ page.body }}</div>
    <div><img src="{{ asset('uploads/images/' ~ page.image) }}" alt="image" class="img-thumbnail"></div>
{% endblock %}